# AutoCode

AutoCode is an autonomous AI developer agent that can plan, implement, and maintain its own codebase.

## Setup
Ensure you have the Gemini CLI installed and configured.

## Usage
- Start a new session in the project directory.
- Use the built-in agents and skills to manage development tasks.
- Project tasks and status are tracked in the `.gemini/` configurations.
